import { css } from "@emotion/css";

export default function StyledSignUpForm(){
    return css`
        width: 100%;
        max-width: 400px;
        display: flex;
        flex-direction: column;
        gap: 6px;
        padding: 0px 12px;
    `
}