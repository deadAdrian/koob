import { css } from "@emotion/react";

export default function StyledNewBookForm(){
    return css`
        min-width: 360px;
        max-width: 400px;
        width: 100%;
        display: flex;
        flex-direction: column;
    `;
}